import React, { Component } from 'react';

class TokenQuantity extends Component {

  constructor(props) {
    super(props);

    this.state = {
      tokens: this.props.tokens
    }
  }

  // Take the current selected tokens
    // Add a toggle (that starts at off)
    // That allows you to give the new contract access
    // to your tokens

  render () {
    return (
      <div div class='form'> 
       TokenQuantity
      </div>
    );
  }
}

export default TokenQuantity;
